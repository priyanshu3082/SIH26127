# Vision integration boundary

Add `detection.py` for YOLO, `ocr.py` for PaddleOCR, `tracking.py` for ByteTrack or BoT-SORT, and `alerts.py` for route anomaly rules. The API currently uses `data/demo_sightings.json` so the full dashboard can be tested without model weights.
